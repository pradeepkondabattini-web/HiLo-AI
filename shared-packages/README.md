# shared-packages/

Cross-cutting packages shared across apps and services: domain models, the API SDK,
and design tokens. Kept dependency-light and framework-agnostic where possible so both
Flutter and Node consumers can reuse them.

Populated incrementally as shared contracts stabilize (starting Sprint 1).
